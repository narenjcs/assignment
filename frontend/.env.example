# Build-time fallback for the API base URL. Used only when public/config.json
# is missing or invalid (see src/lib/config.ts); at deploy time config.json is
# written by CDK with the real Lambda Function URL and takes precedence.
VITE_API_URL=http://localhost:3000
